import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    borderColor: 'red',
    borderWidth: 2,
    alignItems: 'center',
    height: 120,
    justifyContent: 'center',
  }
});
