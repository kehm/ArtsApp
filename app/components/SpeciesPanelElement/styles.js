import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    // borderColor: 'red',
    // borderWidth: 2,
    alignItems: 'center',
    height: 120,
    paddingLeft: 10,
    justifyContent: 'center',
  },
  name: {
    maxWidth: 90,
    paddingTop: 2,
  },
  latinName: {
    maxWidth: 100,
    paddingTop: 2,
    fontSize: 11,
  }
});
