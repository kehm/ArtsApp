import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    headerContainer: {
        backgroundColor: '#5fbb5a'
    },
    title: {
        color: '#000'
    },
    icon: {
        color: '#000'
    },
    left: {
        maxWidth: 50
    },
});
