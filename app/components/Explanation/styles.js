import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    paddingHorizontal: 20,
    alignItems: 'center',
  },
  description: {
    fontSize: 11,
    textAlign: 'center',
  }
});
