import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    paddingHorizontal: 20,
    alignItems: 'center',
  },
  title: {
    paddingBottom: 20,
    textAlign: 'center',
    fontWeight: '100',
  },
  description: {
    fontSize: 12,
    textAlign: 'center',
    fontWeight: '100',
  }
});
