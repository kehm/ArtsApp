import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    paddingHorizontal: 20,
    alignItems: 'center',
  },
  title: {
    paddingBottom: 20,
    textAlign: 'center',
  },
  description: {
    fontSize: 12,
    textAlign: 'center',
  }
});
