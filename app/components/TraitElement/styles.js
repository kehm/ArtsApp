import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: 'yellow',
    borderColor: '#444',
    borderWidth: 1,
    borderRadius: 4,
    padding: 10,
    margin: 10,
  }
});
