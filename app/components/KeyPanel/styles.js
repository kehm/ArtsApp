import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
  },
  swiper: {
  },
  dotStyle: {
    transform: [{ translateY: 5}],
  },
  activeDot: {
    backgroundColor: '#000',
    transform: [{ translateY: 5}],
  }
});
