import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: 60,
    width: 60,
    borderRadius: 30,
    borderWidth: 3,
    borderColor: '#CCC',
    alignItems: 'center',
    justifyContent: 'center'
  },
  image: {
    height: 57,
    width: 57,
    borderRadius: 57 * 0.5,
  }
});
