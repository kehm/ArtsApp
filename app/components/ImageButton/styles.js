import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: 60,
    width: 60,
    borderRadius: 30,
    borderWidth: 6,
    borderColor: '#AAA',
    alignItems: 'center',
    justifyContent: 'center',
    overflow: 'hidden'
  },
  image: {
    height: 58,
    width: 58,
    borderRadius: 58 * 0.5,
  }
});
